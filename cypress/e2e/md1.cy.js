import { SelectablePage } from "../pageObjects/selectablePage";

describe('template spec', () => {
  context('passes', () => {
    beforeEach(() => {
      SelectablePage.visit();
    });

    
  })
})